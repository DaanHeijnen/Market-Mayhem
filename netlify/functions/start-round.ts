import{requireAdmin,audit}from'../lib/auth';import{withTransaction}from'../lib/db';import{body,ok,intValue,HttpError}from'../lib/http';import{incrementGameVersion,setActiveRoundBlock,setScreenMode}from'../lib/game-state';import{wrap}from'./_wrap';
export default wrap(async request=>{const admin=await requireAdmin(request),p=await body<any>(request);const gameId=intValue(p.gameId,'gameId',{min:1}),roundId=intValue(p.roundId,'roundId',{min:1});return ok(await withTransaction(async client=>{const game=await client.query('SELECT current_round_id,prediction_duration_seconds FROM game_nights WHERE id=$1 FOR UPDATE',[gameId]);if(!game.rows[0])throw new HttpError(404,'Game not found');const round=await client.query('SELECT round_number,status FROM rounds WHERE id=$1 AND game_night_id=$2 FOR UPDATE',[roundId,gameId]);if(!round.rows[0])throw new HttpError(404,'Round not found');if(round.rows[0].status!=='UPCOMING')throw new HttpError(409,'Only upcoming rounds can be started');const active=await client.query("SELECT round_number FROM rounds WHERE game_night_id=$1 AND status='ACTIVE' AND id<>$2 FOR UPDATE",[gameId,roundId]);if(active.rows[0])throw new HttpError(409,`Complete R${active.rows[0].round_number} before starting another round`);await client.query("UPDATE rounds SET status='ACTIVE',started_at=NOW(),completed_at=NULL,updated_at=NOW() WHERE id=$1",[roundId]);await client.query('UPDATE game_nights SET current_round_id=$2,current_round_block_id=NULL,updated_at=NOW() WHERE id=$1',[gameId,roundId]);const opened=await client.query(`UPDATE predictions SET status='OPEN',opened_at=NOW(),closes_at=NOW()+($2::text||' seconds')::interval,updated_at=NOW() WHERE game_night_id=$1 AND round_id=$3 AND status='SCHEDULED' RETURNING id`,[gameId,Number(game.rows[0].prediction_duration_seconds),roundId]);const first=await client.query('SELECT id FROM round_blocks WHERE round_id=$1 ORDER BY sort_order,id LIMIT 1',[roundId]);if(first.rows[0])await setActiveRoundBlock(client,gameId,roundId,Number(first.rows[0].id),admin.username);else if(opened.rowCount){const firstPrediction=await client.query("SELECT id FROM predictions WHERE game_night_id=$1 AND round_id=$2 AND status='OPEN' ORDER BY display_number,id LIMIT 1",[gameId,roundId]);await setScreenMode(client,gameId,'PREDICTIONS_OPEN',admin.username,{predictionId:Number(firstPrediction.rows[0].id)});}else await setScreenMode(client,gameId,'DASHBOARD',admin.username);await audit(client,gameId,admin.username,`started R${round.rows[0].round_number}`,'round',roundId,{openedPredictions:opened.rowCount});return{openedPredictions:opened.rowCount,version:await incrementGameVersion(client,gameId)}}));});
