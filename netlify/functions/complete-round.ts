import{requireAdmin,audit}from'../lib/auth';import{withTransaction}from'../lib/db';import{body,ok,intValue}from'../lib/http';import{incrementGameVersion}from'../lib/game-state';import{wrap}from'./_wrap';export default wrap(async r=>{const a=await requireAdmin(r),b=await body<any>(r),g=intValue(b.gameId,'gameId',{min:1}),rid=intValue(b.roundId,'roundId',{min:1});return ok(await withTransaction(async c=>{await c.query(`UPDATE rounds SET status='COMPLETED',completed_at=NOW(),updated_at=NOW() WHERE id=$1 AND game_night_id=$2`,[rid,g]);await audit(c,g,a.username,'completed round','round',rid);return{version:await incrementGameVersion(c,g)}}))});
