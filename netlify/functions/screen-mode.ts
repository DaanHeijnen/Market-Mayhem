import{requireAdmin,audit}from'../lib/auth';import{withTransaction}from'../lib/db';import{body,ok,intValue,textValue}from'../lib/http';import{incrementGameVersion,setScreenMode}from'../lib/game-state';import{wrap}from'./_wrap';export default wrap(async r=>{const a=await requireAdmin(r),b=await body<any>(r),g=intValue(b.gameId,'gameId',{min:1}),mode=textValue(b.mode,'mode');return ok(await withTransaction(async c=>{await setScreenMode(c,g,mode,a.username,b.roundId||null,b.predictionId||null);await audit(c,g,a.username,`screen mode ${mode}`,'screen');return{version:await incrementGameVersion(c,g)}}))});
