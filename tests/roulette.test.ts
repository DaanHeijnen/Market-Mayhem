import{describe,expect,it}from'vitest';import{normalizeRouletteSelection,payoutForStake,rouletteBetWins,roulettePayoutMultiplier}from'../netlify/lib/economy';
describe('roulette rules',()=>{it('validates supported bets',()=>{expect(normalizeRouletteSelection('NUMBER',36)).toBe('36');expect(normalizeRouletteSelection('COLOR','red')).toBe('RED');expect(()=>normalizeRouletteSelection('NUMBER',37)).toThrow()});it('pays straight number at 36x total return',()=>expect(payoutForStake(10,roulettePayoutMultiplier('NUMBER'))).toBe(360));it('resolves even-money bets and zero correctly',()=>{expect(rouletteBetWins('COLOR','RED',1)).toBe(true);expect(rouletteBetWins('PARITY','ODD',1)).toBe(true);expect(rouletteBetWins('RANGE','LOW',18)).toBe(true);expect(rouletteBetWins('COLOR','RED',0)).toBe(false)});it('losing bets receive no payout by rule',()=>expect(rouletteBetWins('NUMBER','12',13)).toBe(false))});
