import{useEffect,useState}from'react';import{useNavigate,useParams}from'react-router-dom';import{api}from'../../lib/api';export function JoinPage(){const{token}=useParams(),nav=useNavigate();const[msg,setMsg]=useState('Joining Market Mayhem…');useEffect(()=>{api<any>('/api/join-player',{method:'POST',body:JSON.stringify({token})}).then(r=>nav(`/play/${r.gameId}`)).catch(e=>setMsg(e.message))},[token]);return <div style={{minHeight:'100vh',display:'grid',placeItems:'center',background:'#DFF24C'}}><h1 className="display">{msg}</h1></div>}
