import{useState}from'react';import type{RunMutation}from'../types';import{Card,Countdown,Empty,Status}from'../ui';
const blank={question:'',roundId:'',yesOdds:'2.00',noOdds:'2.00',scheduled:false};
export function PredictionsPage({state:s,run}:{state:any;run:RunMutation}){const[form,setForm]=useState<any>(blank),[editing,setEditing]=useState<number|null>(null);const save=async()=>{const path=editing?'/api/edit-prediction':'/api/create-prediction';const body={...(editing?{predictionId:editing}:{}),question:form.question,roundId:form.roundId?Number(form.roundId):null,yesOdds:Number(form.yesOdds),noOdds:Number(form.noOdds),scheduled:Boolean(form.scheduled)};if(await run(path,body)){setForm(blank);setEditing(null)}};const begin=(p:any)=>{setEditing(p.id);setForm({question:p.question,roundId:p.round_id||'',yesOdds:String(p.yes_odds),noOdds:String(p.no_odds),scheduled:p.status==='SCHEDULED'})};const screen=(p:any)=>run('/api/screen-mode',{mode:p.status==='OPEN'?'PREDICTIONS_OPEN':p.status==='RESULT'||p.status==='SETTLED'?'PREDICTION_RESULT':'PREDICTION_LOCKED',predictionId:p.id});return <div className="page-stack"><Card><div className="label muted">{editing?'EDIT MARKET':'CREATE MARKET'}</div><div className="form-grid"><label>Question<input className="field" value={form.question} onChange={e=>setForm({...form,question:e.target.value})}/></label><label>Linked round<select className="field" value={form.roundId} onChange={e=>{const id=e.target.value;const r=s.rounds.find((x:any)=>x.id===Number(id));setForm({...form,roundId:id,scheduled:Boolean(id)&&r?.status==='UPCOMING'?form.scheduled:false})}}><option value="">No round</option>{s.rounds.filter((r:any)=>r.status!=='COMPLETED').map((r:any)=><option key={r.id} value={r.id}>R{String(r.round_number).padStart(2,'0')} · {r.title}</option>)}</select></label><label>YES odds<input className="field" type="number" min="1.001" step="0.01" value={form.yesOdds} onChange={e=>setForm({...form,yesOdds:e.target.value})}/></label><label>NO odds<input className="field" type="number" min="1.001" step="0.01" value={form.noOdds} onChange={e=>setForm({...form,noOdds:e.target.value})}/></label></div><label className="check"><input type="checkbox" checked={form.scheduled} disabled={!form.roundId||s.rounds.find((r:any)=>r.id===Number(form.roundId))?.status!=='UPCOMING'} onChange={e=>setForm({...form,scheduled:e.target.checked})}/> Scheduled — automatically open when linked round starts</label><div className="actions"><button className="btn btn-dark" disabled={!form.question.trim()} onClick={save}>{editing?'SAVE MARKET':'CREATE MARKET'}</button>{editing&&<button className="btn" onClick={()=>{setEditing(null);setForm(blank)}}>CANCEL</button>}</div></Card>{s.predictions.length===0?<Empty title="No predictions yet — Create a market"/>:<div className="card-list">{s.predictions.map((p:any)=><Card key={p.id}><div className="row-between"><div><div className="label muted">PREDICTION #{p.display_number}{p.round_number?` · R${String(p.round_number).padStart(2,'0')}`:''}</div><div className="display" style={{fontSize:23}}>{p.question}</div></div><Status>{p.status}</Status></div><div className="odds-row"><div><span>YES</span><b>{p.yes_odds.toFixed(2)}×</b></div><div><span>NO</span><b>{p.no_odds.toFixed(2)}×</b></div><div><span>PARTICIPATION</span><b>{p.participation_count} / {s.players.filter((x:any)=>x.active).length}</b></div>{p.status==='OPEN'&&<div><span>TIME</span><b><Countdown closesAt={p.closes_at}/></b></div>}</div><div className="actions">{['DRAFT','SCHEDULED'].includes(p.status)&&<><button className="btn" onClick={()=>begin(p)}>EDIT</button><button className="btn btn-lime" onClick={()=>run('/api/open-prediction',{predictionId:p.id})}>OPEN NOW</button><button className="btn btn-red" onClick={()=>run('/api/delete-prediction',{predictionId:p.id})}>DELETE</button></>}{p.status==='OPEN'&&<><button className="btn btn-blue" onClick={()=>screen(p)}>SHOW ON SCREEN</button><button className="btn btn-dark" onClick={()=>run('/api/lock-prediction',{predictionId:p.id})}>LOCK NOW</button></>}{p.status==='LOCKED'&&<><button className="btn btn-blue" onClick={()=>screen(p)}>SHOW LOCKED</button><button className="btn" style={{background:'#2FAF5B',color:'white'}} onClick={()=>run('/api/set-prediction-result',{predictionId:p.id,result:'YES'})}>RESULT YES</button><button className="btn btn-red" onClick={()=>run('/api/set-prediction-result',{predictionId:p.id,result:'NO'})}>RESULT NO</button></>}{p.status==='RESULT'&&<><button className="btn btn-blue" onClick={()=>screen(p)}>SHOW RESULT</button><button className="btn btn-dark" onClick={()=>run('/api/settle-prediction',{predictionId:p.id},true)}>SETTLE PAYOUTS</button></>}{!['SETTLED','CANCELLED'].includes(p.status)&&<button className="btn" onClick={()=>run('/api/cancel-prediction',{predictionId:p.id},true)}>CANCEL + REFUND</button>}</div>{p.result&&<p className="muted">Result: <b>{p.result}</b></p>}</Card>)}</div>}</div>}
