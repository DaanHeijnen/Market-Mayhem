import{useState}from'react';import{useLocation,useNavigate}from'react-router-dom';import{useGamePolling}from'../../hooks/useGamePolling';import{api,mutation}from'../../lib/api';import{ControlPage}from'./control/ControlPage';import{SettingsPage}from'./settings/SettingsPage';import{PlayersPage}from'./players/PlayersPage';import{RoundsPage}from'./rounds/RoundsPage';import{PredictionsPage}from'./predictions/PredictionsPage';import{LedgerPage}from'./ledger/LedgerPage';import{MarketPage}from'./market/MarketPage';import{Chip,ChipRow}from'./ui';
type Page='control'|'rounds'|'players'|'predictions'|'market'|'ledger'|'settings';
const PAGES:Page[]=['control','rounds','players','predictions','market','ledger','settings'];
const TITLES:Record<Page,string>={control:'Control Center',rounds:'Rounds',players:'Players',predictions:'Predictions',market:'Market Dashboard',ledger:'Ledger',settings:'Settings'};
export function AdminApp({gameId}:{gameId:number}){const{data:s,error,refresh}=useGamePolling<any>(gameId,'admin',`/api/admin-state?gameId=${gameId}`);const[auth,setAuth]=useState({username:'admin',password:''}),[msg,setMsg]=useState('');const loc=useLocation(),nav=useNavigate();const parts=loc.pathname.split('/').filter(Boolean);const raw=(parts[2]||'control').toLowerCase();const page=((PAGES as string[]).includes(raw)?raw:'control') as Page;const roundId=page==='rounds'&&parts[3]?Number(parts[3]):null;const run=async(path:string,body:Record<string,unknown>,idempotent=false,idempotencyKey?:string)=>{setMsg('');try{await mutation(path,{gameId,...body},idempotent,idempotencyKey)}catch(e){setMsg(e instanceof Error?e.message:'Failed');return false}try{await refresh()}catch(e){setMsg(`Change saved, but live refresh failed: ${e instanceof Error?e.message:'connection error'}`)}return true};if(!s)return <div className="auth-shell"><div className="card auth-card"><div className="label muted">MARKET MAYHEM</div><h1 className="display">Admin Control</h1><p className="muted">Sign in to configure and run the game night.</p><input className="field" placeholder="Username" value={auth.username} onChange={e=>setAuth({...auth,username:e.target.value})}/><input className="field" type="password" placeholder="Password" value={auth.password} onChange={e=>setAuth({...auth,password:e.target.value})}/><button className="btn btn-dark" onClick={async()=>{try{await api('/api/admin-login',{method:'POST',body:JSON.stringify(auth)});location.reload()}catch(e){setMsg((e as Error).message)}}}>SIGN IN</button>{(msg||error)&&<p className="neg"><b>{msg||error}</b></p>}</div></div>;const activeRound=s.rounds.find((r:any)=>r.id===s.game.current_round_id)||null;const activePlayerCount=s.players.filter((p:any)=>p.active).length;const openPredictionCount=s.predictions.filter((p:any)=>p.status==='OPEN').length;return <div className="admin-shell"><aside className="admin-sidebar"><div className="brand">● MARKET MAYHEM</div>{PAGES.map(item=><button key={item} className={page===item?'active':''} onClick={()=>nav(`/admin/${gameId}/${item}`)}>{item.toUpperCase()}</button>)}</aside><main className="admin-main"><header className="admin-header"><div><div className="label muted">{s.game.name}</div><h1 className="display">{TITLES[page]}</h1></div><div className="header-actions"><PresentationToggle state={s} run={run}/><div className={`pill header-state ${s.game.current_round_id ? 'active' : 'idle'}`}>{activeRound?`ROUND ${String(activeRound.round_number).padStart(2,'0')} · ACTIVE`:'NO ROUND ACTIVE'}</div></div></header><ChipRow><Chip tone="white" onClick={()=>nav(`/admin/${gameId}/players`)}>{activePlayerCount} PLAYER{activePlayerCount===1?'':'S'}</Chip><Chip tone="ink" onClick={()=>nav(`/admin/${gameId}/rounds`)}>{activeRound?`ROUND ${String(activeRound.round_number).padStart(2,'0')} · ${activeRound.blocks.length} STEP${activeRound.blocks.length===1?'':'S'}`:'NO ACTIVE ROUND'}</Chip><Chip tone="blue" onClick={()=>nav(`/admin/${gameId}/predictions`)}>{openPredictionCount} PREDICTION{openPredictionCount===1?'':'S'} LIVE</Chip></ChipRow>{page==='control'&&<ControlPage state={s} gameId={gameId} run={run}/>} {page==='players'&&<PlayersPage state={s} gameId={gameId} run={run} setMsg={setMsg}/>} {page==='rounds'&&<RoundsPage state={s} gameId={gameId} roundId={Number.isFinite(roundId)?roundId:null} run={run}/>} {page==='predictions'&&<PredictionsPage state={s} run={run}/>} {page==='market'&&<MarketPage state={s} gameId={gameId} run={run}/>} {page==='ledger'&&<LedgerPage state={s} gameId={gameId}/>} {page==='settings'&&<SettingsPage state={s} run={run} onReset={()=>nav(`/admin/${gameId}/control`)}/>} {(msg||error)&&<div className="toast">{msg||'LIVE CONNECTION INTERRUPTED'}</div>}</main></div>}

// Temporarily show standings, then come straight back to the same step. The round and
// the active block are untouched by this — it only changes what is rendered.
function PresentationToggle({state:s,run}:{state:any;run:(path:string,body:Record<string,unknown>)=>Promise<boolean>}){
  const screen=s.screen||{};
  const onDashboard=screen.mode==='DASHBOARD';
  const canReturn=Boolean(screen.previous?.mode);
  if(onDashboard) return <button className="btn btn-primary btn-compact" disabled={!canReturn} title={canReturn?undefined:'Nothing to return to yet'} onClick={()=>run('/api/restore-screen',{})}>BACK TO RUN OF SHOW</button>;
  return <button className="btn btn-lime btn-compact" onClick={()=>run('/api/screen-mode',{mode:'DASHBOARD',remember:true})}>MARKET DASHBOARD</button>;
}
